---
# Event Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-service
  namespace: {{ .Values.global.namespace }}
  labels:
    app: event-service
spec:
  replicas: {{ .Values.replicaCount.eventService }}
  selector:
    matchLabels:
      app: event-service
  template:
    metadata:
      labels:
        app: event-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 1000
        seccompProfile:
          type: RuntimeDefault
      serviceAccountName: bookmyevent-backend-sa
      containers:
        - name: event-service
          image: {{ .Values.global.imageRegistry }}/event-service:{{ .Values.imageTags.eventService }}
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.eventService.targetPort }}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
            capabilities:
              drop:
                - ALL
          env:
            - name: EVENT_SERVICE_PORT
              value: "{{ .Values.services.eventService.targetPort }}"
            - name: EVENT_SERVICE_DB_URL
              valueFrom:
                secretKeyRef:
                  name: bookmyevent-secrets
                  key: EVENT_SERVICE_DB_URL
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: bookmyevent-secrets
                  key: JWT_SECRET
            - name: INTERNAL_API_KEY
              valueFrom:
                secretKeyRef:
                  name: bookmyevent-secrets
                  key: INTERNAL_API_KEY
            - name: USER_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: bookmyevent-config
                  key: USER_SERVICE_URL
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: bookmyevent-config
                  key: REDIS_URL
            - name: ELASTICSEARCH_URL
              valueFrom:
                configMapKeyRef:
                  name: bookmyevent-config
                  key: ELASTICSEARCH_URL
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: bookmyevent-config
                  key: ENVIRONMENT
          resources:
            {{- toYaml .Values.resources.eventService | nindent 12 }}
          {{- if .Values.healthCheck.enabled }}
          readinessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.services.eventService.targetPort }}
            {{- toYaml .Values.healthCheck.readinessProbe | nindent 12 }}
          livenessProbe:
            httpGet:
              path: /healthz
              port: {{ .Values.services.eventService.targetPort }}
            {{- toYaml .Values.healthCheck.livenessProbe | nindent 12 }}
          {{- end }}
---
# Event Service Service
apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: event-service
  ports:
    - name: http
      port: {{ .Values.services.eventService.port }}
      targetPort: {{ .Values.services.eventService.targetPort }}
  type: {{ .Values.services.eventService.type }}
