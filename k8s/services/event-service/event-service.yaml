---
# Event Service Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: event-service
  namespace: bookmyevent
  labels:
    app: event-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: event-service
  template:
    metadata:
      labels:
        app: event-service
    spec:
      enableServiceLinks: false
      containers:
        - name: event-service
          image: ${AWS_ACCOUNT_ID}.dkr.ecr.${AWS_REGION}.amazonaws.com/bookmyevent/event-service:latest
          ports:
            - containerPort: 8002
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: true
          env:
            - name: EVENT_SERVICE_DB_URL
              valueFrom:
                secretKeyRef:
                  name: bookmyevent-secrets
                  key: EVENT_SERVICE_DB_URL
            - name: REDIS_URL
              valueFrom:
                configMapKeyRef:
                  name: bookmyevent-config
                  key: REDIS_URL
            - name: SEARCH_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: bookmyevent-config
                  key: SEARCH_SERVICE_URL
            - name: ENVIRONMENT
              valueFrom:
                configMapKeyRef:
                  name: bookmyevent-config
                  key: ENVIRONMENT
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8002
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8002
            initialDelaySeconds: 30
            periodSeconds: 30
          volumeMounts:
            - name: env-file
              mountPath: /root/.env
              subPath: .env
      volumes:
        - name: env-file
          configMap:
            name: env-file
---
# Event Service Service
apiVersion: v1
kind: Service
metadata:
  name: event-service
  namespace: bookmyevent
spec:
  selector:
    app: event-service
  ports:
    - port: 8002
      targetPort: 8002
  type: ClusterIP

