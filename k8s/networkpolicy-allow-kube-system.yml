---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-kube-system
  namespace: bookmyevent
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - from:
        - namespaceSelector:
            matchLabels:
              name: kube-system
