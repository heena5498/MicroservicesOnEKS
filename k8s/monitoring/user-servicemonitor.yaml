apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: user-service-monitor
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames:
      - bookmyevent
  selector:
    matchLabels:
      app: user-service
  endpoints:
    - port: 8001
      path: /metrics
      interval: 15s
      scheme: http
