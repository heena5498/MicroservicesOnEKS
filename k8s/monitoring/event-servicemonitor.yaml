apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: event-service-monitor
  namespace: monitoring
spec:
  namespaceSelector:
    matchNames:
      - bookmyevent
  selector:
    matchLabels:
      app: event-service
  endpoints:
    - targetPort: 8082
      path: /metrics
      interval: 15s
