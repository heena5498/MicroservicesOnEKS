# Production Environment
# For deployment on EKS with custom domain
# Empty string means use the same origin as the frontend (recommended for production)
VITE_API_URL=

# Alternative: Use explicit domain
# VITE_API_URL=https://campuseventmanager.work.gd